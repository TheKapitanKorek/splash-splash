import React,{FunctionComponent} from 'react';
import {
    useParams,
  } from "react-router-dom";

interface Props {
    query:"string"
}

export const Photos:FunctionComponent = ()=>{

    return(
        <div>
            <ul>ww</ul>
        </div>
    )
}